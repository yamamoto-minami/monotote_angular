'use strict';

xdescribe('Directive: shoppableView', function () {

  // load the directive's module and view
  beforeEach(module('cmp.shoppableView'));
  beforeEach(module('components/shoppableView/shoppableView.html'));

  //var element, scope;

  //beforeEach(inject(function ($rootScope) {
  //  scope = $rootScope.$new();
  //}));

  //it('should make hidden element visible', inject(function ($compile) {
  //  element = angular.element('<shoppable-view></shoppable-view>');
  //  element = $compile(element)(scope);
  //  scope.$apply();
  //  expect(element.text()).toBe('this is the shoppableView directive');
  //}));
});
